@page
@model Project_smarties.Pages.Quiz.DeleteModel
@using Microsoft.AspNetCore.Identity
@inject UserManager<Models.UserApplicationData> UserDataManager
@inject SignInManager<IdentityUser> SignInManager

@{
    ViewData["Title"] = "delete";
    //redirects the user to the log in menu when not logged in
    if (!SignInManager.IsSignedIn(User))
    {
        Response.Redirect("/Identity/Account/Login");
    }
    //redirects any logged in accounts with the "User" back to the home page
    if (User.IsInRole("user"))
    {
        Response.Redirect("/Index");
    }
    var UserData = await UserDataManager.GetUserAsync(User);
}
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>QuizApplicationData</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.QuizApplicationData.QuizName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.QuizApplicationData.QuizName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.QuizApplicationData.QuizTheme)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.QuizApplicationData.QuizTheme)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.QuizApplicationData.questionCount)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.QuizApplicationData.questionCount)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.QuizApplicationData.questions)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.QuizApplicationData.questions)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.QuizApplicationData.answers)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.QuizApplicationData.answers)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="QuizApplicationData.QuizId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
